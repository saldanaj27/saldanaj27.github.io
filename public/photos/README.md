Family photos for the About page deck.

Drop files named family-1, family-2, family-3 (.jpg, .png, or .webp) here.
The About page renders the deck only when matching files exist, so the build
never ships broken images. Alt text is set in src/pages/about.astro and should
be updated to match whatever the photos actually show.

Keep them small (roughly 640px wide is plenty at the rendered size).
